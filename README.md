# 🌍 Timezone Calculator – API

## 🇧🇷 Português
API desenvolvida em **PHP** responsável por toda a lógica de cálculo de fusos horários. Recebe horários no fuso da empresa e retorna o horário correspondente no fuso local.

### Funcionalidades
- Conversão precisa entre fusos horários
- Detecção de mudança de dia
- Classificação do caso (empresa adiantada, atrasada ou mesmo dia)
- CORS habilitado

### Endpoints
- `GET /api/timezones.php`
- `POST /api/convert.php`

---

## 🇺🇸 English
**PHP API** responsible for all timezone calculation logic. Receives working hours in the company timezone and returns the equivalent local time.

### Features
- Accurate timezone conversion
- Day change detection
- Case classification (company ahead, behind, same day)
- CORS enabled

### Endpoints
- `GET /api/timezones.php`
- `POST /api/convert.php`

---

## 🇪🇸 Español
API desarrollada en **PHP** responsable de toda la lógica de cálculo de zonas horarias. Recibe horarios en la zona horaria de la empresa y devuelve el horario local correspondiente.

### Funcionalidades
- Conversión precisa de zonas horarias
- Detección de cambio de día
- Clasificación del caso (empresa adelantada, atrasada o mismo día)
- CORS habilitado

### Endpoints
- `GET /api/timezones.php`
- `POST /api/convert.php`
